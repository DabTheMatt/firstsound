import { EN, PL } from '../../i18n/messages'

/** Both locales reserve the button so a language change cannot shift the header. */
export const LOAD_SAMPLE_LABELS = [EN.header.loadSample, PL.header.loadSample] as const
